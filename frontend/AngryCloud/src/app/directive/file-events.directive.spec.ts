import { FileEventsDirective } from './file-events.directive';

describe('FileEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new FileEventsDirective();
    expect(directive).toBeTruthy();
  });
});
